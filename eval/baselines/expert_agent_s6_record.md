# 专家 Agent S6：测试后启用验收记录

记录日期：2026-09-15  
实施分支：`expert-agent-s2-price`

## S6 三项交付

1. 单元与契约测试：新增 S6 启用门禁、60 条批测资产、HTTP `reason` 序列化、HTTP→S3 接管、重复消息、生成中卖家接管测试。旧阶段断言已迁移到专家接口，没有删除“不乱答、不漏问、不无故检索”等业务边界。
2. 固定批量问题：原脚本实际只有 50 条，本次补为 60 条。每题使用独立 `chat_id`，记录分类、商品、答案、action、确定性任务规划、来源、缺失原因和耗时。真实 HTTP 批测报告见 `eval/results/expert_agent_s6_batch.json`。
3. 小范围真实验证与启用：新增 fail-closed 启用门禁。受控验收必须限定账号、会话、精确问题和数量，结束自动暂停；正式运行必须提供绑定当前 Git 提交的全通过报告。

## 需求逐项核对

| 需求 | 实现与证据 | 代码验收 |
|---|---|---|
| R01 全问题处理 | 规则任务作为完整性基线；复合、无标点、条件依赖和同专家批处理均有测试；模型不得虚构 greeting | 通过 |
| R02 事实与型号知识隔离 | 商品事实只读当前 MCP／商品介绍；型号知识带 item 范围；common 不替代当前商品服务事实 | 通过 |
| R03 唯一价格政策 | `PriceAgent` 使用整数分，从原价重算，覆盖 1490／1470／1495、售出、冲突、越权条件 | 通过 |
| R04 共享会话 | 复用 `SessionManager.state_json`，追问复用当前商品，切换商品清除交易条件，会话互相隔离 | 通过 |
| R05 确定性合并 | 按原问题顺序合并，价格／状态不经总模型改写，旧 `generate_xianyu` 不参与专家出口 | 通过 |
| R06 统一接管 | 任一任务失败整条只返回“稍等我看看”；reason 经 HTTP 到 S3，通知一次并进入 HUMAN | 通过 |

## A01—A18 自动化证据

| 用例 | 覆盖结论 | 状态 |
|---|---|---|
| A01 | 在售事实直答，不调用 RAG／模型 | 通过 |
| A02 | 包邮最低 1490 | 通过 |
| A03 | 不包邮最低 1470 | 通过 |
| A04 | 同条区分包邮 1490 与不包邮 1470 | 通过 |
| A05 | 接受买家 1495，不主动降价 | 通过 |
| A06 | 1470 包邮固定接管 | 通过 |
| A07 | 无标点句形成在售、维修、价格三任务 | 通过 |
| A08 | 48 小时内／中通，不承诺今天或顺丰 | 通过 |
| A09 | 缺少测光对比记录时接管，不由功能正常推断 | 通过 |
| A10 | 一个关键事实缺失即整条接管，reason 保留缺失与已确认摘要 | 通过 |
| A11 | 同会话“不包邮呢”回答 1470 | 通过 |
| A12 | 不继续报价 1460，越权接管 | 通过 |
| A13 | 切换商品不继承交易条件 | 通过 |
| A14 | 型号知识限制当前商品来源，跨商品来源被拒绝 | 通过 |
| A15 | 卖家确认话术、空结果、错误结构、超时均接管 | 通过 |
| A16 | 重复消息不重复发送；暂停或卖家接管后旧答案不发送 | 通过 |
| A17 | 完整 HTTP 序列化后由模拟 S3 通知一次并进入 HUMAN | 通过 |
| A18 | 原订单、`rag_mcp`、普通 RAG 路由与响应契约未被截走 | 通过 |

## 测试结果

```text
tests/xianyu tests/api tests/orders tests/regression: 294 passed
tests/ingestion: 7 passed
tests/retrieval（不含三组重模型文件）: 39 passed
tests/retrieval/test_chat_retrieval.py（CPU 独立进程）: 2 passed
tests/retrieval/test_qdrant.py（CPU 独立进程）: 3 passed
tests/retrieval/test_vector_search.py（CPU 独立进程）: 3 passed
合计: 348 passed
```

文档要求的单进程 `pytest -q` 实际结果为 `336 passed, 3 failed`。三项失败均发生在 SentenceTransformer 权重加载阶段：Windows 分页文件错误 `os error 1455`／CUDA 4GB 显存不足；拆分到独立 CPU 进程后全部通过。该环境限制没有用 skip／xfail 隐藏。

60 条真实 `/chat` 批测完成，HTTP 与响应安全契约 `60/60`，其中 47 条 reply、13 条 handoff。真实模型抽测发现并修复了两个额外任务问题：A08 不再被模型错误转成 seller-rule RAG，T52 不再被模型虚构 greeting。

## 尚未通过，因此尚未正式启用

- 闲鱼 Qdrant 同步未完成：Docker Desktop WSL 引擎以 `0xc00000fd` 退出，7 条知识类批测因 `knowledge_warmup_failed` 安全接管。批测只能记为“完成”，不能记为语义全通过。
- 当前提交尚未完成真实渠道的正常报价、未知事实接管和生成期间手动接管三项交互；企业微信真实通知也未在本次运行中宣称成功。
- 真实账号已暂停，旧 API／渠道进程已停止。`approved_for_auto_send` 必须保持 `false`，在上述两项通过并生成绑定部署提交的验收报告前，正式运行器拒绝启动。
