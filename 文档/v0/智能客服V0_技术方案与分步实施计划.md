# 智能客服 V1：技术方案与分步实施计划

版本：1.0  
日期：2026-09-07  
依据：《智能客服 V1 需求文档：现有 RAG 问答 + 单个 MCP 订单查询工具》

> 交付目标：同一个 POST /chat，既能通过原有 RAG 回答知识库规则问题，也能通过真正的 MCP 调用查询一笔本地模拟订单。

这是实施方案，不代表已经修改或运行本地仓库。下文新增文件为建议命名；已有文件、类名和目录沿用项目实际结构。

## 一、实现路线与技术选择

执行顺序固定为：

```text
1. 确认现有 RAG 够用
   ↓
2. 实现订单 MCP Server
   ↓
3. 实现 MCP Client，独立验证协议调用
   ↓
4. 实现简单分流
   ↓
5. 接入 /chat 和现有 DeepSeek
   ↓
6. 联调验收，补启动说明，交付 V1
```

第 2 步开始接 MCP；不等待 50 题评估完成。第 3 步证明工具本身可用，第 5 步才让聊天入口调用它。

### 1.1 技术方案

| 部分 | 本版选择 | 修改范围 |
| --- | --- | --- |
| API 入口 | 现有 FastAPI、POST /chat | 不新增另一套聊天 API |
| 知识库问答 | 原有 Embedding、Qdrant、BM25、RRF、Reranker、可靠性判断、Context Builder | 能用就保留 |
| MCP 实现 | 官方 Python SDK `mcp`，本方案按 v2 接口编写 | 新增订单服务端与客户端 |
| MCP 通信 | 本地 stdio，由客户端启动服务端子进程 | 不部署远程服务，不新增 HTTP 端口 |
| 模拟订单 | 服务端文件中的固定 Python 字典，三条记录 | 不新增数据库，也不写入 Qdrant |
| 分流 | Python 普通函数、少量明确规则、订单号提取 | 不新增意图识别模型或 Agent 框架 |
| 订单回答 | 复用现有 DeepSeek 调用，增加一段订单专用提示词 | 不更换模型，不要求模型自主调用工具 |
| 验证 | 相关 pytest + 独立真实 MCP 验证 + 真实 /chat 请求 | 不扩完整评估体系 |

官方文档当前以 Python SDK v2 为稳定线；它提供 `MCPServer` 和 `Client`。本方案不混写 v1 与 v2 的接口，也不额外安装另一套同名工具框架。[1]

```python
# 以下是选定 SDK 的接口名称，不是完整实现。
from mcp.server import MCPServer
from mcp import Client, StdioServerParameters
```

### 1.2 接入后的链路

```text
POST /chat → ChatService → 简单分流
                            │
                            ├─ 规则问题 → 原有 RAG → 原有 DeepSeek → 答案与文档来源
                            │
                            ├─ 订单查询且有订单号
                            │    → MCP Client → stdio → MCP Server.get_order
                            │    → 订单业务数据 → 现有 DeepSeek → 订单回答
                            │
                            ├─ 查订单但缺少订单号 → 直接提示重新发送完整问题
                            │
                            └─ 明确要求执行取消、退款等写操作 → 提示本版不支持
```

一次请求只走一个分支。订单分支不进入 RAG，不经过 Reranker 和 RAG 可靠性阈值，不把订单伪装成知识库文档。

MCP Server 只返回数据；ChatService 决定走哪个分支；DeepSeek 只负责把查到的数据表述成中文。本版不让 DeepSeek 决定是否查订单。

### 1.3 文件落点

原则上只新增两个运行模块：

| 文件或位置 | 职责 |
| --- | --- |
| 新增 `mcp_servers/order_server.py` | 三条模拟数据、工具结果模型、get_order、stdio 服务入口 |
| 新增 `app/infrastructure/order_mcp_client.py` | 启动服务端、连接、调用、提取业务结果、关闭连接 |
| 修改现有 ChatService 所在文件 | 加一个小分流函数和订单分支；保留旧 RAG 路径 |
| 修改现有 prompt 所在文件 | 增加订单回答提示词，不覆盖 RAG 提示词 |
| 必要时修改现有响应模型 | 允许非 RAG 分支不返回 RAG 专属信息 |
| 新增 `scripts/smoke_order_mcp.py` | 独立验证真实工具发现与调用 |
| 新增相关测试，更新 README 和原依赖文件 | 覆盖本次改动，记录启动与演示方法 |

服务端不要导入 `app.main` 或加载 Embedding、Reranker、Qdrant；它只需要 MCP SDK 和三条固定订单。不要为了本版增加 ToolRegistry、AgentManager、通用插件系统或多层适配器。

## 二、逐步实施

## 第 1 步：确认现有 RAG 够用

**目标：确认旧功能能完成真实问答，而不只是返回检索结果。**

只定位 `/chat` 入口、ChatService、DeepSeek 调用、响应模型及现有配置的位置。它们是接入点，不进行全仓库代码审查。

从已校准题目中选择 3 道知识库内问题，其中一道必须是包含“订单”二字的通用规则问题，再加 1 道明显超出知识库的问题。可参考：退货条件、通用发货规则、工单处理时效；具体选择以实际 Markdown 有对应答案为准。

用真正的 `/chat` 检查：知识库内问题返回与资料一致的答案和有效来源；范围外问题沿用原有拒答或转人工标志。

已有同版本端到端成功记录可以复用。只有检索评估结果时，补确认答案生成部分。不重新下载模型，不默认重新入库，不跑全量评估。

**产出：**简短的 3 + 1 验证记录和接入文件位置。

**停止条件：**正常问题能答、来源正确、范围外问题按原逻辑拒答，就进入第 2 步。只有不能运行、普遍查不到或普遍错误拒答等阻断问题才在此修复。

## 第 2 步：实现订单 MCP Server

**目标：把一个读取固定订单的函数，通过 MCP 暴露出去。**

### 2.1 依赖

继续使用项目当前 Python 环境。新增官方 `mcp` 依赖，本方案使用 v2；SDK 要求 Python 3.10 及以上。[1]

```powershell
python -m pip install "mcp>=2,<3"
python -c "from importlib.metadata import version; print(version('mcp'))"
```

把实际验证通过的确切版本写入现有依赖文件或锁文件，不顺手升级 FastAPI、模型库和其他无关依赖。这里不要求安装 MCP Inspector、Node.js 或额外开发平台。

### 2.2 固定模拟数据

把以下三条订单直接保存在 `order_server.py` 的固定字典中：

| order_id | order_status | logistics_status | tracking_no |
| --- | --- | --- | --- |
| TEST1001 | 已发货 | 运输中 | MOCKEXP1001 |
| TEST1002 | 待发货 | 暂无物流 | null |
| TEST1003 | 已签收 | 已签收 | MOCKEXP1003 |

### 2.3 唯一工具

```text
工具名：get_order
输入：order_id，必填字符串
输出：found、order_id、order_status、logistics_status、tracking_no
```

找到订单时返回固定业务数据。没有找到时仍返回正常业务结果：

```json
{
  "found": false,
  "order_id": "TEST9999",
  "order_status": null,
  "logistics_status": null,
  "tracking_no": null
}
```

“查不到订单”不抛连接错误，也不返回一个编造的默认订单。

建议在同一个服务端文件内定义一个简单的 `OrderResult` Pydantic 模型，并让工具返回它。SDK 可据此生成结构化输出，客户端不用从中文句子里反向提取字段。[2]

服务入口采用 `mcp.run(transport="stdio")`，放在 `if __name__ == "__main__"` 内。服务端日志使用 stderr，不向协议 stdout 随意打印业务调试文本。[3]

**产出：**一个轻量服务端文件和依赖记录。

**停止条件：**服务端能够启动，工具和三条数据完成即可；真实协议调用由第 3 步验证。单独运行 stdio 服务端后等待输入是正常现象，不等于已经完成工具调用。[3]

## 第 3 步：实现 MCP Client，先独立调用

**目标：不经过 FastAPI、不经过模型，也能通过真正的 MCP 取到订单。**

### 3.1 客户端只提供一个业务函数

```python
async def get_order_via_mcp(order_id: str) -> dict:
    ...
```

这是待实现的接口约定，不是可直接运行的完整代码。

内部执行以下流程：

```text
使用当前 Python 解释器启动 order_server.py
→ 建立 MCP 客户端连接
→ 完成 SDK 所需的连接协商
→ 调用 get_order，参数为 order_id
→ 检查工具是否执行失败
→ 提取结构化业务结果
→ 关闭连接并回收服务端子进程
```

使用 `StdioServerParameters` 描述启动命令，`Client` 管理连接。启动命令使用 `sys.executable`，脚本使用推导出的绝对路径，参数按列表传递，不拼接一整串带空格的 shell 命令。

**本版选择：每次订单查询新建连接，用完关闭。**不建长连接池，也不把 MCP 连接成功设置成 FastAPI 启动的前提。SDK 的异步上下文管理器负责 stdio 子进程的启动和关闭。[4]

为“启动、连接、调用”设置一个有界等待，例如配置默认 10 秒；这是本地订单工具的调用超时设置，不是响应性能承诺，不限制现有 RAG 模型推理。失败时退出并回收资源，不加自动重试或熔断系统。

### 3.2 区分三种结果

| 结果 | 业务含义 | 后续行为 |
| --- | --- | --- |
| 工具执行正常，found=true | 查到订单 | 返回字段给 ChatService |
| 工具执行正常，found=false | 查完但没有这笔订单 | 返回“未找到”业务结果 |
| 启动、连接、超时或工具执行失败 | 查询没有正常完成 | 向订单分支报告失败 |

按 v2 接口检查 `result.is_error`，成功后读取 `result.structured_content`。工具执行错误可能以结果标志返回，不能只依赖捕获 Python 异常；也不能把异常吞掉后假装成 found=false。[5]

### 3.3 新增独立验证脚本

`scripts/smoke_order_mcp.py` 使用与应用相同的客户端连接方式，真实执行一次 `list_tools()`，确认发现 `get_order`，然后调用三条存在的订单和 TEST9999。

对应文件实现后，在项目根目录执行：

```powershell
python -m scripts.smoke_order_mcp
```

输出至少包含工具名、各查询的 found 和业务字段。脚本应使用断言检查实际值，失败时退出报错，而不是只打印“测试通过”。工具发现只需在此验证，不要求每次 `/chat` 都重新执行 `tools/list`。[5]

**产出：**MCP Client、独立验证脚本和真实运行记录。

**停止条件：**能发现工具、三条数据查对、TEST9999 返回 found=false。此时 MCP 已独立打通，可以接聊天入口；不要继续扩第二个工具。

## 第 4 步：实现一个简单分流函数

**目标：区分问通用规则与查具体订单。**

在现有 service 文件内增加普通函数即可：

```text
route_query(query) → (route, order_id)
```

`route` 只服务本版，可使用 `rag`、`order`、`missing_order_id`、`unsupported_action` 四种内部结果，不另建通用路由体系。

| 输入示例 | 结果 |
| --- | --- |
| 退货需要什么条件？ | rag |
| 订单一般多久发货？ | rag |
| 帮我查订单 TEST1001 发货了吗？ | order，TEST1001 |
| 帮我查一下我的订单。 | missing_order_id |
| 帮我取消订单 TEST1001。 | unsupported_action |
| 退款条件是什么？ | rag，而不是 unsupported_action |

实现顺序：先识别清晰的执行写操作请求；再识别具体订单状态或物流查询并提取订单号；明确查个人订单但缺少有效订单号时提示补充；其余问题继续原有 RAG。

只支持需求文档中的清晰表达。订单号采用 TEST 加 4 位数字，英文大小写可统一成大写。不要把出现“订单”当作唯一条件，也不要把出现“退款”当作一定要求执行退款。

缺少订单号时，请求用户重新发送完整问题。本版不实现“上一轮问订单、这一轮只发编号”的会话记忆。复杂多意图与隐式指代不扩入验收。

新增一个小测试文件覆盖上表。分流测试不需要加载模型，不连接 Qdrant，也不调用真实 DeepSeek。

**产出：**一个分流函数及相关测试。

**停止条件：**表中输入走对分支。不要继续升级成意图分类模型。

## 第 5 步：接回 /chat，复用现有 DeepSeek

**目标：同一个入口真正完成两种服务。**

### 5.1 修改 ChatService 的执行顺序

```text
读取 query
→ 调用分流函数

rag：直接走原有 RAG 逻辑
missing_order_id：直接返回补充提示
unsupported_action：直接返回不支持提示
order：await get_order_via_mcp(order_id)
       ├─ found=false → 直接返回未找到
       ├─ 调用失败 → 直接返回失败
       └─ found=true → 使用订单提示词调用现有 DeepSeek → 返回回答
```

使用 MCP SDK 的异步调用方式接入现有请求链路；不要在已经运行的异步处理函数内再次启动一个顶层事件循环。仅调整此次接入涉及的调用，不进行全项目异步重构。[5][8]

### 5.2 增加一个订单提示词

复用原 DeepSeek 的配置、密钥和调用代码，只新增一种输入模板，不新写第二套模型客户端：

```text
你是订单查询助手。下面的数据来自本地模拟订单查询工具。
只根据提供的订单字段回答，不补充未提供的到货时间、地址、金额等信息。
订单号、订单状态、物流状态和运单号必须与数据一致。
运单号为空时说明暂无运单号，不输出字面量 null。
回答中明确标识“本地模拟订单数据”。

用户问题：{query}
订单数据：{order_data_json}
```

订单数据作为结构化 JSON 注入该模板，不经过原知识库 Context Builder。缺号、未找到、查询失败、不支持操作均直接回复，不必调用模型。生成失败时明确“本次订单查询回答失败”，不返回看似成功的业务回答。

### 5.3 最小适配返回结构

请求继续使用：

```json
{"query": "帮我查订单 TEST1001 发货了吗？"}
```

RAG 分支的响应保持原样。建议非 RAG 分支沿用现有字段并按下表约定：

| 字段 | 非 RAG 分支约定 |
| --- | --- |
| query | 原始用户问题 |
| answer | 订单回答或明确提示 |
| sources | []，不伪造 Markdown 来源 |
| results | 若原响应已有此字段，则返回 [] |
| reliability | null，不伪造检索评分 |
| next_step | null，不让工具查询套用 RAG 的处理步骤 |
| can_answer | 正常查到并完成回答为 true；明确查无订单也为 true；缺号、不支持、调用或生成失败为 false |

这里 can_answer=true 表示本次查询已得到明确可反馈的结果，不表示订单一定存在。上述是本版建议约定，不是对现有模型定义的描述；现有模型若不允许非 RAG 分支的空值，只做对应字段的必要适配。

不新增另一套 ChatResponse 或通用状态系统，不为了订单查询伪造高分通过 RAG 可靠性阈值。

新增少量接口/服务测试，可用替身替代 MCP 和 DeepSeek 来检查分支行为及响应结构；它们不能替代第 3 步和第 6 步的真实运行。

**产出：**可用的订单聊天分支、一个订单提示词、最小响应适配与相关测试。

**停止条件：**从 `/chat` 查询 TEST1001 能得到基于真实 MCP 返回值整理的回答，普通规则问题仍走原有 RAG。

## 第 6 步：联调验收并交付

**目标：按需求文档 A1—A7 验收，不再扩大功能。**

### 6.1 本地运行

保持现有 Qdrant 与模型配置。以下以原 FastAPI 入口为 `app.main:app` 举例；实际入口沿用项目现状。

```powershell
python -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

Windows 首次 stdio 联调采用单进程、不带 `--reload` 的方式。Uvicorn 在 Windows 热重载或多 worker 模式下的事件循环选择可能与异步子进程不兼容；单进程先把主链路跑通，不为此扩建运行平台。[6][7]

不用手动再打开一个终端保持订单 MCP 服务端运行，订单客户端会启动它。[4]

对应文件完成后，可先运行小测试与独立验证，再发送真实聊天请求：

```powershell
python -m pytest tests/test_order_routing.py tests/test_order_chat.py -q
python -m scripts.smoke_order_mcp
```

测试文件名按实际实现落位，另复跑此次影响到的原有 `/chat` 相关测试，不要求补齐所有历史测试。

```powershell
$body = @{ query = "帮我查订单 TEST1001 发货了吗？" } | ConvertTo-Json
Invoke-RestMethod `
  -Uri "http://127.0.0.1:8000/chat" `
  -Method Post `
  -ContentType "application/json; charset=utf-8" `
  -Body ([System.Text.Encoding]::UTF8.GetBytes($body)) |
  ConvertTo-Json -Depth 10
```

### 6.2 验收清单

| PRD 编号 | 场景 | 通过条件 |
| --- | --- | --- |
| A1 | 3 道知识库问题，其中一道是通用订单规则 | 走 RAG，答案与资料一致，来源有效；通用规则不调用 MCP |
| A2 | 1 道明显超出知识库的问题 | 保留现有拒答或转人工标志 |
| A3 | 独立真实 MCP 验证 | 发现 get_order，并通过协议调用得到数据 |
| A4 | 从 /chat 查询 TEST1001、TEST1002、TEST1003 | 状态、物流、运单号准确，明确是本地模拟数据 |
| A5 | 查询 TEST9999 | 明确没有这笔订单，不说成连接失败，不编造状态 |
| A6 | 明确查订单但未提供编号 | 提示重发完整问题，不调用 MCP |
| A7 | 人为制造一次 MCP 启动或调用失败 | 返回明确失败；普通 RAG 问答仍可使用 |

不支持写操作的提示随第 4、5 步的小测试确认，不增加写工具。

A7 可以这样验证：API 启动后，临时将服务端脚本改名，使下一次订单请求无法启动工具；发送订单查询，再发送一道普通知识库问题；最后恢复文件名并确认订单恢复。记录真实结果，不把仅模拟抛异常的测试当作全部故障验收。

使用现有日志记录 `route=rag` 或 `route=order`、工具名及 found 结果即可；不增加完整调用轨迹平台。

### 6.3 最小交付

提交必要代码、相关小测试、三条固定订单、依赖记录及一份简短验收记录。README 写清配置、启动方式、真实 MCP 验证命令和聊天示例即可。

**停止条件：A1—A7 通过，新加功能没有破坏旧 RAG，即完成 V1。**不再等待完整评估指标、覆盖率数字、全部边界测试或所有历史问题清零。

## 三、出现问题时，只回到对应层

| 现象 | 当前检查位置 |
| --- | --- |
| 独立 MCP 验证失败 | 订单服务端、客户端、依赖和子进程启动 |
| 独立验证成功，但 /chat 未调用 MCP | 分流函数及 ChatService 接入 |
| 工具数据正确，但回答状态错误 | 订单提示词和 DeepSeek 输入 |
| 新增功能使原 RAG 问答失效 | 此次对 ChatService 或响应模型的修改 |

不因为订单工具失败就去重新调整 Embedding、RRF 或 Qdrant。已有未相关的代码冗余、排序细节和风格问题记入待办，不在本次顺手重构。

## 四、当前交给 Codex 的第一条指令

```text
请先阅读智能客服 V1 需求文档和实施计划。当前只执行第 1 步：确认现有 RAG 主链路够用。

先定位 /chat、ChatService、DeepSeek 调用和响应模型的实际文件位置，不做全仓库审查。
复用同版本已有的端到端成功记录；不足的部分，验证 3 道知识库问题和 1 道范围外问题。
三个知识库问题中要包含一个“订单一般多久发货”这类通用规则问题，以实际知识库支持的题目为准。
只修复阻断这些问答的问题；不扩评估、不重构、不重新下载模型，不开始后续步骤。

完成后说明：验证了哪些问题、实际结果、修改了哪些文件（如有）、能否进入第 2 步。
没有实际运行的部分明确标注“未运行”，不要把静态检查写成验证通过。
```

后续每次只执行一个步骤，达到该步骤停止条件后继续。六个步骤是六次小改动，不是六轮全面审查。

## 技术参考

项目范围、三条订单、路由规则、验收场景及超时默认值属于本项目设计；以下资料只核对所选 SDK 与运行方式，不增加需求。

[1] [MCP Python SDK 官方文档](https://py.sdk.modelcontextprotocol.io/)

[2] [MCP Python SDK：Structured Output](https://py.sdk.modelcontextprotocol.io/servers/structured-output/)

[3] [MCP Python SDK：Running your server](https://py.sdk.modelcontextprotocol.io/run/)

[4] [MCP Python SDK：Client transports](https://py.sdk.modelcontextprotocol.io/client/transports/)

[5] [MCP Python SDK：The Client](https://py.sdk.modelcontextprotocol.io/client/)

[6] [Uvicorn：Event Loop](https://uvicorn.dev/concepts/event-loop/)

[7] [Python：asyncio Subprocesses](https://docs.python.org/3/library/asyncio-subprocess.html)

[8] [Python：asyncio Runners](https://docs.python.org/3/library/asyncio-runner.html)
