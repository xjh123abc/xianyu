# 智能客服 V1 需求文档

现有 RAG 问答 + 单个 MCP 订单查询工具

版本 1.0  |  2026-09-07  |  本地演示范围

**版本目标：同一个 `/chat` 入口，能根据知识库回答规则问题，也能通过 MCP 查询一笔模拟订单。**

## 01 目标与当前起点

在已有 V0 上增加一条订单查询分支，不重建智能客服平台。**主链路可用、关键结果正确，就进入下一步；不把所有小问题修完作为前置条件。**

依据此前反馈，项目已搭起 RAG 主链路，并开始做检索评估。本文件以该进度为起点，不代表本次重新检查了仓库或运行了本地代码。

继续使用现有 12 份 Markdown、本地 Embedding 与 Reranker、Qdrant、BM25、RRF、FastAPI 和 DeepSeek。保留已有配置、评估脚本及结果，不要求换模型或重新入库。

## 02 现有链路与本版目标链路

**知识库准备：**Markdown → Loader → Chunker → Embedding → Qdrant；文本内容同时供 BM25 检索使用。

**现有问答：**问题 → `/chat` → ChatService → 向量检索与 BM25 → RRF → Rerank → Answer Reliability。证据足够时，经 Context Builder 和 DeepSeek 返回答案与来源；证据不足时，沿用现有拒答或转人工标志。

```text
POST /chat → ChatService → 简单分流

规则问题 → 现有 RAG → DeepSeek → 答案 + 文档来源

订单查询 → MCP Client → 本地 MCP Server → get_order
         → 模拟订单数据 → DeepSeek → 查询回答

明确要查订单，但缺少订单号 → 直接提示补充
```

**分流放在 RAG 之前。**订单工具不经过知识库召回、重排和可靠性阈值；知识库没有某笔订单，不应阻止查询工具。一次请求只走一个分支，本版不做“查订单后再联合规则推理”。

范围限定为本地、单用户演示、单次完整问题、一个只读工具和三条模拟订单。现有 API 即为演示入口，不新增前端。

## 03 功能需求

### F1｜保留并确认现有 RAG 问答

从现有已校准题目中选择 **3 道知识库内问题 + 1 道明显超出知识库的问题**，通过真正的 `/chat` 入口检查。知识库内问题应能得到符合资料的答案和来源；范围外问题应沿用现有拒答行为。

三个知识库问题中，应包含一个带“订单”二字、但实际是在问通用规则的问题。已有对应版本的端到端成功记录可以沿用；只跑过检索评估的，补确认答案生成即可。**达到这一条件就进入 MCP，不等待 50 题全量评估。**

### F2｜简单分流，不建设 Agent 框架

| 输入场景 | 本版行为 |
| --- | --- |
| “退货需要什么条件？”<br>“订单一般多久发货？” | 走现有 RAG；不能只因出现“订单”就调用工具。 |
| “帮我查订单 TEST1001 发货了吗？” | 提取订单号，进入 MCP 订单查询分支。 |
| “帮我查一下我的订单。” | 直接提示提供订单号，不调用工具，不进入 RAG 猜测。 |

首版只要求覆盖清晰的订单查询表达，演示订单号采用 `TEST` 加 4 位数字。缺少订单号时，提示用户重新发送完整问题，例如“帮我查订单 TEST1001”；不依赖上一轮会话记忆。

退款、取消订单、修改地址等操作不执行；明确提示本版仅支持订单状态查询。复杂多意图和隐式指代不属于本版验收范围。

### F3｜通过真正的 MCP 调用一个工具

客服项目自身接入 MCP Client，本地 MCP Server 只暴露 `get_order(order_id)`。本版选择 `stdio` 通信，由客户端启动服务端子进程，不部署远程 MCP 服务。[1]

应完成实际连接与初始化，并至少用一次真实运行证明：可以通过 `tools/list` 发现 `get_order`，再通过 `tools/call` 取得模拟订单数据。[2] **直接调用普通 Python 函数，或只给 Codex 配置 MCP，都不算本需求完成。**

由程序的简单规则决定何时调用工具，暂不要求 DeepSeek 自主选工具。服务端只读取模拟数据，不连接真实订单系统，不执行任何订单变更。

### F4｜基于查询结果回答，不编造业务状态

查到订单时，把工具返回的业务数据交给现有 DeepSeek 调用能力整理成中文回答，并标明来自本地模拟订单。订单号、订单状态、物流状态和运单号应与数据一致；未提供的到货时间等信息不补写。

缺少订单号、订单不存在、工具调用失败时，直接给出对应提示即可，不必经过模型。工具或生成调用失败，不得声称查询成功；明确失败即可，不扩展自动重试、复杂降级或错误平台。

## 04 工具数据与接口约定

### 4.1 唯一工具与模拟订单

工具名称固定为 `get_order`，唯一必填参数为字符串 `order_id`。本地固定保存以下三条模拟订单；使用一份 JSON 或项目内固定数据即可，不新增数据库。

| order_id | 订单状态 | 物流状态 | 运单号 |
| --- | --- | --- | --- |
| TEST1001 | 已发货 | 运输中 | MOCKEXP1001 |
| TEST1002 | 待发货 | 暂无物流 | 空 |
| TEST1003 | 已签收 | 已签收 | MOCKEXP1003 |

以下为本项目约定的业务结果示例，不是 MCP 协议的完整报文；协议封装交给所选 SDK。

```json
{
  "found": true,
  "order_id": "TEST1001",
  "order_status": "已发货",
  "logistics_status": "运输中",
  "tracking_no": "MOCKEXP1001"
}
```

未找到订单时返回 `found=false`，保留查询的 `order_id`，其余三个业务字段为 `null`。**“没有这笔订单”是查询结果，不等同于连接或执行失败。**待发货订单的运单号同样为 `null`。

### 4.2 保留 `/chat` 入口，最小适配返回

继续使用 `POST /chat`，请求仍通过 `query` 传入完整问题，不新增另一套聊天入口。现有 RAG 返回结构保留；新分支只对当前响应模型做必要适配，不顺带重构接口。

```json
{"query": "帮我查订单 TEST1001 发货了吗？"}
```

RAG 回答继续返回现有文档 `sources`。订单分支通过 `answer` 返回查询回答，文档 `sources` 为空，不伪造 Markdown 来源；使用一句说明标识“本地模拟订单数据”。

现有 `can_answer`、`next_step`、`reliability` 等字段只做与分支语义一致的最小适配：RAG 可靠性结果只描述 RAG，不用它否决订单查询，也不为工具伪造检索分数。本版不另建一套通用状态系统。

### 4.3 回答示例

**查到订单：**根据本地模拟订单数据，订单 TEST1001 已发货，当前物流状态为运输中，运单号为 MOCKEXP1001。

**缺少订单号：**请提供订单号，并重新发送完整问题，例如：帮我查订单 TEST1001。

**订单不存在：**未查询到模拟订单 TEST9999，请核对订单号。

**调用失败：**本次订单查询失败，请稍后重试。

以上是语义示例，不要求逐字一致。用现有日志或一份简短测试记录说明实际走了哪个分支、是否调用 MCP 即可，不增加完整调用轨迹平台。

## 05 最小验收与执行顺序

**通过下表即可结束本版。**不设 Recall、MRR、测试覆盖率或并发指标门槛；已有验收记录可复用，但必须包含一次真实 MCP 调用，不能全用模拟测试替代。

| 编号 | 验收场景 | 通过条件 |
| --- | --- | --- |
| A1 | 3 道知识库内问题<br>含一道通用订单规则问题 | 走 RAG，答案与资料一致，来源有效；通用订单规则问题不调用 MCP。 |
| A2 | 1 道明显超出知识库的问题 | 沿用现有拒答或转人工标志；不要求接入真人客服。 |
| A3 | 真实 MCP 工具验证 | 发现 get_order，并通过协议实际调用成功，不是普通函数或纯 mock 测试。 |
| A4 | 从 /chat 分别查询三条模拟订单 | 回答中的订单、物流及运单信息与数据一致，注明模拟数据。 |
| A5 | 查询 TEST9999 | 明确未找到订单，不编造状态，不说成连接失败。 |
| A6 | 明确查订单，但未给订单号 | 提示重新发送带订单号的完整问题，不调用订单工具。 |
| A7 | 人为制造一次 MCP 调用失败 | 订单请求返回明确失败，不一直卡住；之后正常 RAG 问答仍可使用。 |

**执行顺序：**先确认 RAG 的 3 + 1 个基本问答，再独立打通 `get_order` 的真实 MCP 调用，最后接入 `/chat` 并按上表联调。每一步达到停止条件就继续，不回头扩做完整评估或全面审查。

## 06 不做事项与交付边界

**本版不做：**50 题全量评估及完整指标体系；为未来换模型做兼容；全项目重构；多工具 Agent、多轮记忆与复杂意图分类；真实订单平台、退款等写操作；前端、真人客服工单、企业部署与高并发优化。已有评估资料保留。

**只处理阻断当前目标的问题：**主链路无法完成、业务结果明显错误、新功能破坏原有问答。个别排序、措辞、代码风格和无关边界问题，简短记入待办，不阻止交付。

**最小交付物：**必要代码变更、三条模拟订单、相关小测试，以及 README 中的配置、启动和演示命令。测试只覆盖本次改动和关键旧链路，不追求把所有历史测试或所有边界补齐。

保持现有目录和组件，能复用就复用，不因“以后可能用到”增加通用框架。接入真实用户订单前另行补齐身份与订单归属校验；本版仅用于本地模拟演示。

**完成定义：同一个 `/chat` 能完成知识库问答和 MCP 模拟订单查询，上述关键场景通过，即交付 V1。未列入本版的优化，不自动加入本次开发。**

## 技术参考

以下仅用于核对 MCP 协议，不增加本版需求。本地数据、分流与验收数量均为本项目约定。

[1] [MCP 官方 Transports / stdio](https://modelcontextprotocol.io/specification/2025-06-18/basic/transports)

[2] [MCP 官方 Tools](https://modelcontextprotocol.io/specification/2025-06-18/server/tools)
