# 项目目录整理记录

> 本记录集中保存目录整理的盘点、迁移清单和验收结果。本次仅完成五部分计划的第一部分：确认范围、保护现状、建立基线；未移动、删除业务资料，也未修改业务逻辑。

## 本次执行：第一部分

### 起点与工作区保护

| 项目 | 已核对的结果 |
| --- | --- |
| 执行日期 | 2026-09-17 |
| 仓库根目录 | `D:/rag demo智能客服/ecommerce-ai-customer-service` |
| 整理前分支 | `xianyu-dev` |
| 整理前提交 | `aee96c8554cc59db927e61b930e90edb8f6ceb30` |
| 整理分支 | `chore/project-layout-cleanup`，从上述提交创建 |
| Git 已跟踪文件数 | 177 |
| 工作区状态 | 整理开始前已有未提交改动；全部保持原状，未暂存、未提交、未重置 |

以下差异均在本轮整理开始前已存在，归属未在本轮推断或改写：

| 状态 | 路径 | 本轮处理 |
| --- | --- | --- |
| 已修改 | `.env.example` | 保留，不覆盖 |
| 已删除 | `review/Code Review Report - 初步核心链路审查.md` | 保留当前工作区状态，不恢复、不提交 |
| 新增未跟踪 | `eval/results/expert_agent_s6_sample_10.json` | 保留，不移动 |
| 新增未跟踪 | `review/深度 Code Review 报告.md` | 保留，不移动 |
| 新增未跟踪 | `sql_practice.py` | 保留，不移动 |
| 新增未跟踪 | `文档/闲鱼客服机器人/整理/闲鱼项目目录整理_五部分实施计划.md` | 保留，作为本轮实施依据 |

### 私有备份与回退点

在创建整理分支前，已在项目目录外的私有位置建立备份：`D:/rag demo智能客服/_private_project_backups/ecommerce-ai-customer-service/20260917T110429/`。备份目录不进入 Git；本记录不保存 Cookie、API Key、Webhook 或聊天原文。

| 受保护对象 | 源路径 | 备份方式与核验 |
| --- | --- | --- |
| 环境配置 | `.env` | 私有副本 |
| 普通电商入库清单 | `.rag_ingestion_manifest.json` | 私有副本 |
| 闲鱼入库清单 | `.xianyu_ingestion_manifest.json` | 私有副本 |
| 会话记忆库 | `chat_sessions.sqlite3` | `sqlite3.Connection.backup()`；`PRAGMA integrity_check` 为 `ok` |
| 闲鱼渠道控制库 | `logs/xianyu_stage3.sqlite3` | `sqlite3.Connection.backup()`；`PRAGMA integrity_check` 为 `ok` |
| 必要运行证据 | `logs/` 下的 10 个 `.log`/`.json` 文件 | 私有副本；未读取或写入本记录中的聊天、Cookie 等内容 |

盘点时检测到闲鱼/HTTP 运行器相关 Python 进程仍在运行。第一部分没有修改运行代码、数据库配置或业务资料，因此没有停止监听器；后续若要修改这些对象，必须先停止监听器并再次确认备份。

### 实际路径与依赖边界

| 对象 | 当前实际路径 | 本轮决定 |
| --- | --- | --- |
| 普通电商知识库 | `data/ecommerce_rag_test_data/ecommerce_rag_test_data/knowledge_base/` | 保留；后续迁移到 `data/ecommerce/knowledge/`，不下线普通电商链路 |
| 普通电商题集 | `data/ecommerce_rag_test_data/ecommerce_rag_test_data/eval_questions.json` | 保留；后续迁移到 `data/ecommerce/eval_questions.json`，不与其他题集合并 |
| 普通电商资料说明 | `data/ecommerce_rag_test_data/ecommerce_rag_test_data/README.md` | 保留；后续迁移到 `data/ecommerce/README.md` |
| 闲鱼商品事实 | `data/xianyu/items.json` | 原位保留，不改商品事实 |
| 闲鱼知识资料 | `data/xianyu/knowledge/` | 原位保留，不与普通电商资料混合 |
| 入库测试样例 | `data/raw/ingestion_test.txt` | 保留；后续迁移到 `tests/fixtures/ingestion_test.txt`，并同步测试引用 |
| 会话记忆库 | `chat_sessions.sqlite3`（`SESSION_DATABASE_PATH`） | 原位保护；不以空库替换 |
| 渠道控制库 | `logs/xianyu_stage3.sqlite3`（当前 API 与运行脚本硬编码默认值） | 原位保护；第三部分才统一配置入口 |
| 外部参考代码 | `.runtime/xianyu-template` | 存在，提交为 `540bbc26cf02ee6348d997843942776a9be9460b`；本轮不移动、不删除、不更新 |

当前运行配置解析到的资料路径与上表一致：普通电商和闲鱼分别使用独立知识路径；会话记忆库为根目录 `chat_sessions.sqlite3`。当前没有可用的 `XIANYU_CHANNEL_DATABASE_PATH` 配置，渠道控制库仍由 `app/api/conversations.py` 在导入时固定指向 `logs/xianyu_stage3.sqlite3`。

### 保留与后续处理清单

下表是后续部分执行时的唯一迁移清单；本次所有条目的处理状态均为“未执行”。

| 原路径或对象 | 目标路径或归类 | 理由 | 需同步修改的引用 | 状态 |
| --- | --- | --- | --- | --- |
| `app/` | 原位保留 | 运行代码主结构 | 仅第四部分可能抽取一处共享逻辑 | 保留，不执行 |
| `config/` | 原位保留 | 第三部分才更新资料路径和渠道数据库配置 | 配置读取方与测试 | 保留，不执行 |
| `scripts/` | 原位保留 | 现有启动命令必须可继续使用 | 命令入口与文档 | 保留，不执行 |
| `mcp_servers/` | 原位保留 | 同时承载模拟订单和商品查询工具 | MCP smoke 与调用方 | 保留，不执行 |
| `data/xianyu/` | 原位保留 | 闲鱼商品与知识资料须独立 | 闲鱼 RAG、商品事实读取 | 保留，不执行 |
| `data/ecommerce_rag_test_data/ecommerce_rag_test_data/knowledge_base/` | `data/ecommerce/knowledge/` | 普通电商链路仍使用，路径过深 | 配置、loader、入库与检索测试 | 待第三部分 A |
| `data/ecommerce_rag_test_data/ecommerce_rag_test_data/README.md` | `data/ecommerce/README.md` | 与普通电商资料同处 | 数据集说明链接 | 待第三部分 A |
| `data/ecommerce_rag_test_data/ecommerce_rag_test_data/eval_questions.json` | `data/ecommerce/eval_questions.json` | 保留原题集边界 | 评测脚本 | 待第三部分 A |
| `data/raw/ingestion_test.txt` | `tests/fixtures/ingestion_test.txt` | 是入库测试样例，不是无用原始资料 | `tests/ingestion`、`tests/retrieval` | 待第三部分 A |
| `tests/` | 原位保留 | 防止整理造成回归 | 测试命令 | 保留，不执行 |
| `eval/` | `eval/baselines/`、`eval/results/local/` 等后续分类 | 需区分题集、基线、验收模板和临时结果 | 评测脚本与忽略规则 | 待第二部分 |
| `文档/`、`review/` | `文档/当前/`、`文档/历史/`、`文档/审查/` | 先逐份阅读再分类，禁止整目录机械移动 | Markdown 相对链接与 README 导航 | 待第二部分 |
| `.runtime/` | 原位保留 | 是固定版本参考代码，不是缓存 | 运行器 `--reference-root` | 保留，不执行 |
| `.agents/`、`skills-lock.json`、`.vscode/` | 原位保留 | 开发辅助与编辑器配置 | 无 | 保留，不执行 |
| `.env`、SQLite、`logs/`、入库记录 | 原位保护 | 包含密钥或运行状态，且被 Git 忽略 | 私有备份和实际运行器 | 保留，不执行 |
| `requirements.txt`、`pytest.ini`、`conftest.py`、`.gitignore` | 原位保留 | 目录整理不顺便升级依赖或重写测试环境 | 开发与测试环境 | 保留，不执行 |

旧普通电商知识 Markdown 是普通电商 RAG 的回答资料，后续只迁移到清晰位置；`文档/` 下的 PRD、技术方案和学习记录不是默认客服知识库，只能分类或归档，不能导入问答知识库。第一部分没有删除候选文件。

### 测试基线

为避免 `app/api/conversations.py` 在导入时直接打开 `logs/xianyu_stage3.sqlite3`，完整测试在项目外的私有副本中运行。副本不包含生产日志、SQLite 数据库、Git 元数据或 `.runtime/`；测试生成的状态只存在于副本内。

| 项目 | 结果 |
| --- | --- |
| 命令 | `D:/conda_envs/rag-customer-service/python.exe -m pytest -q` |
| 运行位置 | 私有隔离副本 `baseline-test-20260917T110429` |
| 结果 | 360 通过，8 失败，耗时 38.64 秒 |
| 日志 | 私有副本中的 `pytest-baseline-run.out.log` 与 `pytest-baseline-run.err.log` |

已知失败（整理前基线，不在第一部分修复）：

- `tests/xianyu/test_xianyu_complex_buyer_messages.py`：6 项失败。
- `tests/xianyu/test_xianyu_expert_orchestrator.py::test_missing_model_result_is_fail_closed`。
- `tests/xianyu/test_xianyu_structured_facts.py::test_bargain_within_automatic_limit_replies_from_original_price`。

### 本次边界与结论

- 实际改动：新建本记录；创建整理分支；在项目外创建私有备份与隔离测试副本。
- 保持不变：业务代码、资料位置、商品事实、检索范围、提示词、价格授权、转人工规则、数据库配置和正式监听器行为。
- 待处理：第二部分才建立 README 导航、项目索引和文档分类；第三部分才移动资料并更新引用；第四部分才处理共享代码和旧代码结论。

结论：第一部分完成。现状已有私有备份和可追溯测试基线，未移动或删除业务资料；在核对本清单前不进入第二部分。
