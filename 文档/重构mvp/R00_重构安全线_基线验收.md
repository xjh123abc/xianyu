# R00：重构安全线与基线验收

> 状态：自动化基线已完成
> 验收日期：2026-09-18
> 原则：本阶段只修复运行环境与测试稳定性，不新建平行客服系统，也不迁移业务主链路。

## 固定的运行环境

| 用途 | 解释器/配置 | 结果 |
| --- | --- | --- |
| 自动化测试与本地模型运行 | `D:\conda_envs\rag-customer-service\python.exe`（Python 3.12.14，CUDA PyTorch 2.6.0+cu124） | MCP、FastAPI、pytest、Qdrant 均已安装，`pip check` 通过；GPU 双模型真实推理通过 |

不使用全局 Python 3.14 作为项目解释器：它缺少项目所需的 MCP 与 PyTorch，不能作为可靠基线。之前临时使用过 CPU 环境做资源对照，但不属于项目标准环境。

## GPU 结论

当前配置的两个本地模型可以同时驻留并在 GPU 上推理：

- `Qwen3-Embedding-0.6B`：成功生成 1024 维向量；
- `Qwen3-Reranker-0.6B`：成功完成重排打分；
- 两者同时加载后的显存：约 2281 MB 已分配、2314 MB 已保留；GPU 总显存为 4096 MB。

项目新增 `MODEL_DEVICE` 配置，默认 `auto`：优先 CUDA；CUDA 不可用或 CUDA/显存加载失败时回退 CPU。可显式设为 `cuda` 或 `cpu`。非 GPU 的模型配置、文件或代码错误不会被回退逻辑掩盖。

可复核 GPU 结果：

```powershell
& "D:\conda_envs\rag-customer-service\python.exe" scripts\verify_local_models.py --device cuda
```

## 自动化回归基线

执行命令：

```powershell
& "D:\conda_envs\rag-customer-service\python.exe" -m pytest -q `
  --junitxml=eval\results\r00_pytest_original_env.xml
& "D:\conda_envs\rag-customer-service\python.exe" -m scripts.smoke_order_mcp
```

结果：

```text
387 passed in 33.75s
MCP stdio smoke: passed
```

JUnit 记录：`eval/results/r00_pytest_original_env.xml`。

覆盖的既有能力包括：

- `/chat` API 与普通 RAG；
- 商品事实、闲鱼单问题与多问题；
- SQLite Session、TTL 与跨实例锁；
- 订单路由，以及真实本地 MCP stdio 子进程；
- 闲鱼 Adapter、发送器边界、去重、AUTO/HUMAN 控制与控制台接口。

为消除 Windows 上非确定性的资源失败，测试启动时将 OpenBLAS/OMP/MKL/NumExpr 线程限制为 1；该设置仅存在于 `conftest.py`，不改变生产服务的线程策略。

## 后续迁移时的断言分类

以下旧测试当前是“现状基线”，但在后续需求实现时应有意识地替换，不能误判为意外回归：

| 后续需求 | 现有行为 | 迁移后的目标 |
| --- | --- | --- |
| R07 部分成功 | 任一专家任务失败可导致整轮 handoff | 保留已确认答案，将未确认部分明确标记 |
| R08 取消自动 HUMAN | 澄清、RAG/Agent 异常、空答案会转 `human_handoff` | 自动流程只 `reply` / `clarify` / `ignore` / `error`，Session 保持 `AUTO` |

其他基线测试在每个迁移步骤后必须继续通过。

## 真实闲鱼发送

自动化已覆盖发送边界和受控验收门禁，但本次 R00 没有向真实闲鱼会话发送消息。真实发送必须由卖家提供并确认专用测试账号、测试会话和允许发送的原文后，在 `--controlled-acceptance` 模式执行；不得对未知买家或生产会话试发。
